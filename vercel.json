{
  "builds": [
    { "src": "web/package.json", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "web/$1" }
  ],
  "installCommand": "npm install --workspaces --include-workspace-root",
}
